version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: meu_postgres
    environment:
      POSTGRES_DB: meudb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret123
    ports:
      - "5432:5432"